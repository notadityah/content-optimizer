%-------------------------
% Modern Cover Letter Template
% Minimal, tech-focused design
%------------------------

\documentclass[letterpaper,11pt]{article}

\usepackage{latexsym}
\usepackage[empty]{fullpage}
\usepackage{titlesec}
\usepackage[usenames,dvipsnames]{color}
\usepackage{verbatim}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage{fancyhdr}
\usepackage[english]{babel}
\usepackage{tabularx}
\usepackage{geometry}

% Page setup
\geometry{
    letterpaper,
    left=0.75in,
    right=0.75in,
    top=0.75in,
    bottom=0.75in
}

\pagestyle{fancy}
\fancyhf{}
\fancyfoot{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

\urlstyle{same}
\raggedbottom
\raggedright
\setlength{\tabcolsep}{0in}
\setlength{\parindent}{0pt}
\setlength{\parskip}{8pt}

%-------------------------------------------
%%%%%%  COVER LETTER STARTS HERE  %%%%%%%%%%%%

\begin{document}

%----------HEADING----------
\begin{center}
    \textbf{\Huge \scshape <<name|escape_latex>>}
    \vspace{4pt}

    <@ if links and links|length > 0 @>\small <@ for link in links @><@ if not loop.first @> $|$ <@ endif @>\href{<<link.url>>}{<<link.display|escape_latex>>}<@ endfor @><@ endif @>
\end{center}

\vspace{12pt}

%----------RECIPIENT (OPTIONAL)----------
<@ if recipient_name or recipient_title or company @>
\textbf{<@ if recipient_name @><<recipient_name|escape_latex>><@ endif @><@ if recipient_title @><@ if recipient_name @>, <@ endif @><<recipient_title|escape_latex>><@ endif @>}\\
<@ if company @><<company|escape_latex>><@ endif @>

\vspace{8pt}
<@ endif @>

%----------SUBJECT (OPTIONAL)----------
<@ if subject @>
\textbf{Re: <<subject|escape_latex>>}

\vspace{8pt}
<@ endif @>

%----------BODY----------
<# Split on blank lines so each paragraph becomes a real LaTeX \par (parskip applies). #>
<@ if body @>
<@ for paragraph in body.strip().split('\n\n') @>
<@ if paragraph.strip() @>
<<paragraph.strip()|escape_latex>>

<@ endif @>
<@ endfor @>
<@ endif @>

<@ if closing @>
\vspace{12pt}
<@ for paragraph in closing.strip().split('\n\n') @>
<@ if paragraph.strip() @>
<<paragraph.strip()|escape_latex>>

<@ endif @>
<@ endfor @>
<@ endif @>

\end{document}
